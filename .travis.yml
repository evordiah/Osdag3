language: python

python:
    - 3.7

sudo: required
dist: trusty
before_install:
    -  sudo apt-get install texlive-base
install:
    - pip install -r requirements.txt

script:
    - python Module_test.py
